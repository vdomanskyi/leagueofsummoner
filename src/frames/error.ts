import { Fields } from '../interfaces/other.interface';
import { Data } from '../interfaces/other.interface';
import { Assets } from '../interfaces/other.interface';

export default async (assets: Assets, { user, matchIds }: Data, fields: Fields) => {};
